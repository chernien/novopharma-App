import { RecommandePipe } from './recommande.pipe';

describe('RecommandePipe', () => {
  it('create an instance', () => {
    const pipe = new RecommandePipe();
    expect(pipe).toBeTruthy();
  });
});
